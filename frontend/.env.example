# Copy to .env.local for dev or .env.production for Pages build.
# Leave unset to use same-origin "/api" (works with Vite proxy and
# Pages deployments where the Worker is routed onto the same zone).
#
# Example for standalone Worker + Pages deployments:
# VITE_API_BASE=https://fpl-oracle-api.example.workers.dev/api
